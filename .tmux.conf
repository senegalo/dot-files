set-option -g prefix C-b
unbind-key C-a
unbind-key C-Right
unbind-key C-Left
setw -g xterm-keys on
bind-key C-b last-window
unbind-key tab
set -g base-index 1

# clear screen
bind -n C-k send-keys -R \; clear-history

#set -g default-command /usr/local/bin/fish
#set-option -g default-shell /usr/local/bin/fish

set -g default-terminal "xterm-256color"
set-option -g default-terminal "xterm-256color"
set-option -g set-titles on
set -g set-titles-string "#P"
setw -g mode-keys vi
setw -g monitor-activity on
setw -g main-pane-width 96
bind F10 confirm-before kill-server



# set-option -g status-justify centre
set-option -g status-justify left
set-window-option -g window-status-current-bg red
set-option -g status-bg "#2d2f30" #black
set-option -g status-fg white
set-option -g status-left-length 40
set-option -g history-limit 16384

# default statusbar colors
set-option -g status-bg colour18 # "#2d2f30"  #colour18 #base02
set-option -g status-fg colour252 #yellow
set-option -g status-attr default

# default window title colors
set-window-option -g window-status-fg colour252
set-window-option -g window-status-bg "#2d2f30"  # colour21

# active window title colors
set-window-option -g window-status-current-fg colour252 #orange
set-window-option -g window-status-current-bg default

# message text
set-option -g message-bg colour235 #base02
set-option -g message-fg colour166 #orange

# clock
set-window-option -g clock-mode-colour colour64 #green

unbind-key j
bind-key j select-pane -D 

unbind-key k
bind-key k select-pane -U

unbind-key h
bind-key h select-pane -L

unbind-key l
bind-key l select-pane -R

unbind-key b
bind-key b setw synchronize-panes

######################
### DESIGN CHANGES ###
######################

# panes
set -g pane-border-fg black
set -g pane-active-border-fg brightred

## Status bar design
# status line
set -g status-justify left
set -g status-bg "#2d2f30"  #default
set -g status-fg colour12
set -g status-interval 2

# messaging
set -g message-fg black
set -g message-bg yellow
set -g message-command-fg blue
set -g message-command-bg black

#window mode
setw -g mode-bg "#2d2f30" #colour6
setw -g mode-fg colour16

# window status
setw -g window-status-format " #F#I:#W#F "
setw -g window-status-current-format " #F#I:#W#F "
setw -g window-status-format "#[fg=magenta]#[bg=black] #I #[bg=cyan]#[fg=colour8] #W "
setw -g window-status-current-format "#[bg=brightmagenta]#[fg=colour8] #I #[fg=colour8]#[bg=colour14] #W "
setw -g window-status-current-bg colour16
setw -g window-status-current-fg colour11
setw -g window-status-current-attr dim
setw -g window-status-bg green
setw -g window-status-fg black
setw -g window-status-attr reverse

# Info on left (I don't have a session display for now)
set -g status-left ''

# loud or quiet?
set-option -g visual-activity off
set-option -g visual-bell off
set-option -g visual-silence off
set-window-option -g monitor-activity off
set-option -g bell-action none

set -g default-terminal "screen-256color"

# The modes {
setw -g clock-mode-colour colour135
setw -g mode-attr bold
setw -g mode-fg colour196
setw -g mode-bg colour238

# }
# The panes {

set -g pane-border-bg default #colour16 # "#2d2f30" # colour235
set -g pane-border-fg default #colour16 #colour238
set -g pane-active-border-bg default #colour16 #"#2d2f30" # colour236
set -g pane-active-border-fg default #colour16 #colour238 #colour51

# }
# The statusbar {

set -g status-position bottom
set -g status-bg colour24 # "#2d2f30" #colour234
set -g status-fg colour137
set -g status-attr dim
set -g status-left ''
set -g status-right '#[fg=colour233,bg=colour241,bold] %d/%m #[fg=colour233,bg=colour245,bold] %H:%M:%S '
set -g status-right-length 50
set -g status-left-length 20

setw -g window-status-current-fg colour81
setw -g window-status-current-bg colour24 # "#2d2f30" #colour238
setw -g window-status-current-attr bold
setw -g window-status-current-format ' #I#[fg=colour250]:#[fg=colour255]#W#[fg=colour50]#F '

setw -g window-status-fg colour138
setw -g window-status-bg colour24 # "#2d2f30" # colour235
setw -g window-status-attr none
setw -g window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '

setw -g window-status-bell-attr bold
setw -g window-status-bell-fg colour255
setw -g window-status-bell-bg colour24 # "#2d2f30" #  colour1

# }
# The messages {

set -g message-attr bold
set -g message-fg colour232
set -g message-bg colour166

# }

# mouse scroll please
set -g mouse on
# to enable mouse scroll, see https://github.com/tmux/tmux/issues/145#issuecomment-150736967
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'copy-mode -e'"


# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'git@github.com/user/plugin'
# set -g @plugin 'git@bitbucket.com/user/plugin'
set -g @plugin 'tmux-plugins/tmux-yank'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

bind-key -T copy-mode-vi 'v' send -X begin-selection
